//Exercise 2-2. Write a loop equivalent to the for loop above without using && or || .
#include <stdio.h>
#define MAX_LEN 1000
int main() {
	char c;
	char s[MAX_LEN};
    for (i=0; i < lim-1 ; ++i)
    {
    
    c=getchar()
    if (c==EOF)break;
    if (c=='\n') break;
	s[i] = c;
	}
    return 0;
}

